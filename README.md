# 🐹 햄톡 (HamTalk) — 카카오톡 스타일 채팅 앱

햄스터 캐릭터 이모티콘 20종이 포함된 실시간 채팅 애플리케이션입니다.
1:1 / 단체 대화, 이미지·파일 공유, 화려한 SVG 스티커 기능을 제공합니다.

---

## 📋 목차

1. [주요 기능](#-주요-기능)
2. [시스템 요구사항](#-시스템-요구사항)
3. [설치 가이드](#-설치-가이드)
4. [실행 방법](#-실행-방법)
5. [사용 방법](#-사용-방법)
6. [폴더 구조](#-폴더-구조)
7. [환경 변수](#-환경-변수)
8. [문제 해결 (FAQ)](#-문제-해결-faq)

---

## ✨ 주요 기능

| 기능 | 설명 |
|------|------|
| 🔐 회원가입/로그인 | JWT 인증, 비밀번호 bcrypt 해싱 |
| 💬 1:1 채팅 | 사용자 검색 후 즉시 대화 시작 |
| 👥 단체 대화 | 여러 명을 초대해 그룹방 생성, 도중 멤버 추가 가능 |
| ⚡ 실시간 메시지 | Socket.io 기반 즉시 전송/수신 |
| 🖼️ 이미지 공유 | 채팅에서 이미지 미리보기 및 라이트박스 |
| 📎 대용량 파일 공유 | 기본 5GB까지 (env로 조정 가능), 진행률·속도·남은시간 실시간 표시 |
| 🎬 동영상 / 🎵 오디오 | 채팅 안에서 바로 재생 (Range 요청 스트리밍 지원) |
| 🐹 햄스터 스티커 | 그라데이션·글로우·파티클 효과의 SVG 이모티콘 20종 |
| ✏️ 타이핑 표시 | 상대방이 입력 중일 때 실시간 인디케이터 |
| 🟢 온라인 상태 | 접속 중인 사용자를 초록 점으로 표시 |
| 🔢 안읽은 메시지 카운트 | 노란 배지로 미확인 개수 표시 |
| 📅 날짜 구분선 | 날짜별로 메시지 그룹핑 |

---

## 💻 시스템 요구사항

- **Node.js**: 16.x 이상 (권장: 18.x 또는 20.x)
- **npm**: 8.x 이상
- **OS**: Windows 10+, macOS 10.15+, Linux (Ubuntu 20.04+)
- **브라우저**: 최신 Chrome, Edge, Firefox, Safari
- **디스크 공간**: 약 500MB (node_modules 포함)

> Node.js 설치: https://nodejs.org/ko/download/

---

## 🛠️ 설치 가이드

### 1단계: 저장소 클론

```bash
git clone <repository-url>
cd Chatbot
```

또는 이미 받은 폴더로 이동:
```bash
cd Chatbot
```

### 2단계: 서버 의존성 설치

프로젝트 루트에서 실행:
```bash
npm install
```

설치되는 주요 패키지:
- `express` — 웹 서버 프레임워크
- `socket.io` — 실시간 양방향 통신
- `better-sqlite3` — 임베디드 데이터베이스
- `multer` — 파일 업로드
- `bcryptjs` — 비밀번호 해싱
- `jsonwebtoken` — 토큰 기반 인증

### 3단계: 클라이언트 의존성 설치

```bash
cd client
npm install
cd ..
```

설치되는 주요 패키지:
- `react` — UI 라이브러리
- `socket.io-client` — Socket.io 클라이언트
- `axios` — HTTP 통신
- `date-fns` — 날짜 포맷

### 4단계: 환경 변수 설정 (선택)

프로젝트 루트의 `.env` 파일을 확인/수정합니다:

```env
NODE_ENV=production
PORT=5000
JWT_SECRET=원하는-비밀-키로-변경하세요
```

> **⚠️ 운영 환경에서는 반드시 `JWT_SECRET`을 변경하세요!**

### 5단계: React 앱 빌드

```bash
npm run build
```

`client/build` 폴더에 최적화된 정적 파일이 생성됩니다.

---

## 🚀 실행 방법

### 방법 1: 프로덕션 모드 (권장)

빌드된 React 앱을 Express가 함께 서빙합니다.

```bash
npm start
```

성공 시 출력:
```
서버 실행 중: http://localhost:5000
```

브라우저에서 **http://localhost:5000** 접속.

### 방법 2: 개발 모드 (실시간 코드 변경 반영)

서버와 React 개발 서버를 동시에 실행:

```bash
npm run dev
```

- 서버: http://localhost:5000
- React 개발 서버: http://localhost:3000 (자동 열림)

코드 수정 시 자동으로 새로고침됩니다.

### 서버 종료

터미널에서 `Ctrl + C` 입력.

---

## 📖 사용 방법

### 1. 회원가입

1. 브라우저에서 **http://localhost:5000** 접속
2. 노란색 로그인 화면에서 **[회원가입]** 탭 클릭
3. 다음 정보를 입력:
   - **아이디**: 영문/숫자 (고유해야 함)
   - **비밀번호**: 자유롭게 설정
   - **닉네임**: 다른 사용자에게 표시될 이름
4. **[회원가입]** 버튼 클릭 → 자동 로그인

> 💡 **다른 사람과 채팅하려면 친구도 회원가입을 해야 합니다.**
> 같은 컴퓨터에서 테스트하려면 시크릿/프라이빗 창에서 다른 계정으로 접속하세요.

### 2. 로그인

1. **[로그인]** 탭 선택
2. 아이디/비밀번호 입력 후 **[로그인]** 버튼
3. 토큰은 7일간 자동 유지 (다음 접속 시 자동 로그인)

### 3. 새 대화 시작

#### 1:1 대화
1. 좌측 상단 **✏️ 버튼** 클릭
2. 검색창에 친구의 아이디 또는 닉네임 입력
3. 결과에서 한 명을 클릭하여 선택
4. **[대화 시작 (1명)]** 버튼 클릭

#### 단체 대화
1. **✏️ 버튼** 클릭
2. 여러 사용자를 검색하여 선택 (선택된 사람은 노란 칩으로 표시)
3. 상단에 **그룹 대화방 이름** 입력 (선택사항)
4. **[대화 시작 (N명)]** 버튼 클릭

> 💡 같은 사람과의 1:1 대화방은 한 개만 존재합니다. 다시 시작하면 기존 방으로 이동합니다.

### 4. 메시지 보내기

채팅방을 선택하면 우측에 대화창이 열립니다.

#### 📝 텍스트
- 하단 입력창에 메시지 입력 → **Enter** 또는 **➤ 버튼**
- **Shift + Enter** = 줄바꿈

#### 🐹 햄스터 스티커
1. 입력창 왼쪽 **🐹 버튼** 클릭
2. 20종 스티커 패널이 열립니다 (4열 그리드)
3. 원하는 스티커를 클릭하면 즉시 전송

**스티커 목록:**
| | | | |
|:-:|:-:|:-:|:-:|
| 사랑가득 ❤️ | 무지개 🌈 | 빵터짐 😂 | 펑펑 😭 |
| 폭발화남 🔥 | 경악 😱 | 잠자기 😴 | 수줍 😊 |
| 쿨가이 😎 | 파이팅 💪 | 고민중 🤔 | 챔피언 🏆 |
| 축하파티 🎉 | 먹구름 🌧️ | 천사 😇 | 마법사 🧙 |
| 공주님 👑 | 안아줘 🤗 | 음악 🎵 | 게이머 🎮 |

#### 📎 파일/이미지 첨부
1. 입력창의 **📎 버튼** 클릭
2. 파일 선택 (이미지/PDF/Word/Excel/ZIP 등)
3. 자동 업로드 후 채팅에 표시
4. **이미지는 클릭하면 라이트박스로 확대**
5. **파일은 클릭하면 다운로드**

> 📦 **대용량 파일 지원: 기본 최대 5GB** (env `MAX_FILE_SIZE`로 조정 가능)
> 업로드 도중 **진행률·속도(MB/s)·남은 시간**이 채팅창 상단에 실시간 표시됩니다.
> 🎬 **동영상(mp4/webm/mov 등)** 은 채팅에서 바로 재생 가능 (Range 스트리밍)
> 🎵 **오디오(mp3/wav/m4a 등)** 는 인라인 플레이어로 재생
> ⛔ 보안상 차단 형식: `.exe .bat .cmd .com .scr .msi .dll .sh .app .deb .rpm`

### 5. 그룹방에 멤버 추가

1. 채팅방 우측 상단 **👥 버튼** 클릭
2. 현재 참여자 목록 확인
3. **[+ 대화 상대 추가]** 클릭
4. 사용자 검색 → **[추가]** 버튼

> 💡 1:1 대화방에 사람을 추가하면 자동으로 그룹방으로 전환됩니다.

### 6. 상대방 상태 확인

- **사이드바 프로필 사진 우하단의 초록 점** = 현재 접속 중
- **타이핑 표시** = "OOO님이 입력 중..." (자동 사라짐)
- **읽지 않은 메시지** = 채팅방 옆 **노란 배지** (숫자)

### 7. 로그아웃

좌측 상단 **🚪 버튼** 클릭.

---

## 📁 폴더 구조

```
Chatbot/
├── server/                    # 백엔드 (Node.js + Express)
│   ├── index.js               # 서버 진입점 (Socket.io 포함)
│   ├── database.js            # SQLite 스키마 정의
│   ├── middleware/
│   │   └── auth.js            # JWT 인증 미들웨어
│   └── routes/
│       ├── auth.js            # 회원가입/로그인/검색 API
│       ├── rooms.js           # 채팅방/메시지/멤버 API
│       └── upload.js          # 파일 업로드 API
│
├── client/                    # 프론트엔드 (React)
│   ├── public/                # 정적 파일
│   ├── src/
│   │   ├── App.js
│   │   ├── index.js
│   │   ├── index.css          # 글로벌 스타일 + 애니메이션
│   │   ├── contexts/
│   │   │   ├── AuthContext.js     # 로그인 상태 관리
│   │   │   └── SocketContext.js   # Socket 연결 관리
│   │   └── components/
│   │       ├── AuthPage.js         # 로그인/회원가입
│   │       ├── MainLayout.js       # 메인 레이아웃
│   │       ├── Sidebar.js          # 채팅방 목록
│   │       ├── ChatRoom.js         # 채팅창
│   │       ├── CreateRoomModal.js  # 새 대화 생성
│   │       ├── MemberModal.js      # 멤버 관리
│   │       ├── ImageViewer.js      # 이미지 뷰어
│   │       ├── Emoticons.js        # 햄스터 스티커 20종 SVG
│   │       └── EmoticonPicker.js   # 스티커 선택 패널
│   └── build/                 # 빌드 결과물 (생성됨)
│
├── uploads/                   # 업로드된 파일 저장 (자동 생성)
├── chatapp.db                 # SQLite DB (자동 생성)
├── package.json               # 서버 의존성
├── .env                       # 환경 변수 (직접 작성)
└── README.md                  # 이 문서
```

---

## ⚙️ 환경 변수

`.env` 파일에 설정 가능한 항목:

| 변수 | 기본값 | 설명 |
|------|--------|------|
| `PORT` | 5000 | 서버 포트 번호 |
| `NODE_ENV` | development | `production` 설정 시 빌드된 React 앱을 함께 서빙 |
| `JWT_SECRET` | (내장 키) | JWT 서명용 비밀키 — **운영 환경에서는 반드시 변경** |
| `MAX_FILE_SIZE` | 5368709120 (5GB) | 업로드 가능한 최대 파일 크기 (바이트). 예: `10737418240` = 10GB |

---

## ❓ 문제 해결 (FAQ)

### Q1. 포트 5000이 이미 사용 중이라고 나옵니다

다른 프로세스가 점유 중입니다. 다음 중 하나를 선택:

**A) 다른 포트 사용:**
`.env` 파일에서 `PORT=5001`로 변경 후 재실행.

**B) 점유 프로세스 종료 (Linux/macOS):**
```bash
kill $(lsof -ti:5000)
```

**Windows:**
```cmd
netstat -ano | findstr :5000
taskkill /PID <PID번호> /F
```

### Q2. `npm install` 도중 `better-sqlite3` 빌드 에러가 납니다

C++ 컴파일러가 필요합니다.

**Windows:**
```bash
npm install --global windows-build-tools
```

**macOS:**
```bash
xcode-select --install
```

**Linux (Ubuntu):**
```bash
sudo apt install build-essential python3
```

### Q3. 친구가 같은 와이파이에서 접속하려면?

서버를 실행한 PC의 IP 주소를 확인:
- Windows: `ipconfig`
- macOS/Linux: `ifconfig` 또는 `ip addr`

친구는 같은 네트워크에서 **http://[당신의IP]:5000** 으로 접속.
방화벽에서 5000번 포트 허용이 필요할 수 있습니다.

### Q4. 회원가입한 계정 정보를 모두 초기화하고 싶어요

서버 종료 후:
```bash
rm chatapp.db
rm -rf uploads/*
```
다시 시작하면 DB가 새로 생성됩니다.

### Q5. 업로드한 파일은 어디에 저장되나요?

프로젝트 루트의 `uploads/` 폴더에 UUID 파일명으로 저장됩니다.
원본 파일명·크기·타입은 DB에 기록됩니다.

### Q6. 비밀번호를 잊었습니다

현재 비밀번호 재설정 기능이 없습니다. DB에서 해당 계정을 삭제하고 재가입하세요:
```bash
sqlite3 chatapp.db "DELETE FROM users WHERE username='your_id';"
```

### Q7. HTTPS로 운영하고 싶어요

Nginx 등 리버스 프록시 뒤에 두는 것을 권장합니다. 또는 `https` 모듈로 직접 SSL 인증서를 적용해 `server/index.js`를 수정하세요.

### Q8. 모바일에서도 동작하나요?

반응형은 아직 최적화되지 않았습니다. 데스크톱 브라우저 또는 태블릿 가로 모드를 권장합니다.

---

## 📜 라이선스 / 기여

이 프로젝트는 학습 및 데모 목적으로 제작되었습니다.

**기술 스택:**
- 백엔드: Node.js, Express, Socket.io, SQLite, JWT, Multer
- 프론트엔드: React 18, Socket.io-client, Axios, date-fns
- 디자인: 카카오톡 스타일 (노란색 #FEE500 테마)

---

**즐거운 채팅 되세요! 🐹💛**
